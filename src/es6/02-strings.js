let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world + '!';

// Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// Multi-line strings
let lorem = 'Esto es un parrafo \n' + 'esto es el otro parrafo';

let lorem2 = `Esto es una frase epica
y esto es otra frase epica`;

console.log(lorem);
console.log(lorem2);